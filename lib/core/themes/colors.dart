import 'package:flutter/material.dart';

class AppColors {
  // Light
  static const lightText = Color(0xFF2B2B2B);
  static const lightBackground = Color(0xFFE3E3E3);

  // Dark
  static const darkText = Color(0xFFE3E3E3);
  static const darkBackground = Color(0xFF2B2B2B);

  // Shared
  static const primary = Color(0xFFB29AD0);
  static const secondary = Color(0xFFFFC2CD);
  static const accent = Color(0xFFA7DADC);
}